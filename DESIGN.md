# Lighthouse · 灯塔 — 设计（v0）

> 2026-09-18 立项构思会定稿。目标用户：**水平很低的同学**（微信级——不能碰命令行/配置/英文术语）。

## 一句话

一个说人话的 AI 小助手：桌面窗口，打开即聊，零学习成本。

**目标能力**（2026-09-18 用户定）：聊天问答 + 写东西 + **读写 PPT / Word / Excel** + 技术任务；AI 的活动范围锁在**工作区文件夹**内（围栏红线：只有放进去的文档才能被读改）；DSH 引擎只在 exe 内部启动，不出现网页形态；对话规则（skill）由用户撰写。

## 约束（用户定的红线）

- 内存 ≤ 300MB，CPU/GPU 克制 → 无粒子、无循环动画，动效只做一次性微交互
- 审美不能差 → 深海设计语言（与 Cola Hub / 博客同一宇宙：深底 / 玻璃 / Signal Blue）
- 分发态零配置：key 由站长提供并预置，用户无感知

## 形态

- Electron 无边框窗口（480×760 竖版，自绘标题栏）
- **单会话**：无历史列表、无侧栏、无设置——像微信跟一个人单聊
- 纯手写 HTML/CSS/JS，零框架（与博客/Hub 一致）

## 技术决定（2026-09-18）

| 决定 | 理由 |
|---|---|
| 前端直连 `api.deepseek.com`，主进程不做 API 代理 | 参考 yif2012/deepseek-chat-electron 验证 CORS 开放，实现最简 |
| SSE 解析带缓冲区 | 参考项目用 `split('\n')` 有跨 chunk 丢数据的 bug，我们修正 |
| 不显示 `reasoning_content` | 对低水平用户，一句「深度思考中…」占位比满屏思维链友好（v0.4：开着深度思考时收流但只显示占位，正文一到就替换） |
| key 读取：本地 config.json → Hub settings.json 兜底 | 开发态零配置；分发态预置 config.json |
| 模型：默认 `deepseek-flash` + 「深度思考」开关切 `deepseek-v4-pro`（2026-09-19 修正：`deepseek-chat` 已失效） | 日常快且便宜；同学遇难题自己按开关升级（9/19 定；v0.4 已接线，实测 models 端点只此两个 id） |
| 长对话：超阈值自动压缩，**AI 主动告知**（不悄悄丢历史） | 同学不知道「上下文」为何物，透明化处理 + 重要记忆落文件才安全（9/19 定） |

## 引擎通道验证（2026-09-19 上午实测）

**结论：干活引擎 = DSH headless spawn；聊天 = DeepSeek API 直连 —— 混合架构。**

| 测试 | 结果 | 用时 |
|---|---|---|
| T1 纯聊天 `dsh --profile headless "只回复：收到"` | ✅ stdout 干净输出 | 5s |
| T2 带工具（写文件） | ✅ 文件真创建 + 自读取验证 + 汇报 | 6s |
| T3 跨进程记忆 | ❌ 每次 spawn = 全新会话（源码实锤：每次 `session-<uuid>` 新生成，无 resume 旗标） | — |

- **干活路径**：spawn `dsh --profile headless "<任务>"`（cwd=工作区）；stdout=最终回复、exit code=成功与否、stderr=错误；多轮上下文用**注入式**（历史拼进任务文本——DeepSeek 前缀缓存保证低成本）。v0.7 起改为 `spawn(process.execPath, [本地 DSH, …], { env: 摘掉 CHROME_*/ELECTRON_* + ELECTRON_RUN_AS_NODE=1 + DSH_HOME })`（见「内嵌 DSH」）
- **8 月 MCP 通道的工具挂死问题，headless 通道不存在**（T2 实锤）
- **配置面** = `~/.dsh/profiles/headless/cordis.patch.yml`（现为空 `[]`）——沙箱/权限/人格/技能全在这层挂
- **✅ E2E 干活闭环（同日 10:20 实测）**：灯塔里说「建 demo.txt」→ 路由 `[动手]` → 后台 spawn DSH（cwd=工作区）→ 「好，这就去弄…」→ 15 秒后结果气泡 + 文件真实落地。学生视角 = DSH 完全隐形。

**DSH 现成模块 ↔ 产品红线对照**（180+ 包冰山）：

| 红线 | DSH 模块 |
|---|---|
| 工作区围栏 | `dsh-sandbox` / `dsh-fs-sandbox` / `dsh-sandbox-windows-acl` |
| 确认卡 | `dsh-user-approval` / `dsh-tool-ask-user` / `dsh-permission-presets` |
| 上下文压缩 | `dsh-compaction`（UI 层负责"主动告知"） |
| 规则.md | `dsh-skill-filesystem` / `dsh-agent-instructions` / `dsh-persona` |
| 计划→确认→执行 | `dsh-plan-mode` |

⚠️ `HUB_CAPABILITY` env 在 DSH 侧无实现（Hub 留的坑位）——权限不能靠它，要靠 cordis patch 或 UI 层两段式。

## AI 人格（Cola 本人口吻）

**说话风格 ≈ Cola 本人**（9/19 夜定）：短句口语直接、不客套 / 先结论后展开 / 轻度吐槽幽默（「这波可惜」「先别半场开香槟」味，偶尔一句别堆）/ 讲技术打比方说人话 / 不会就说不会；对不懂电脑的同学保留耐心版（操作一步步、术语顺带解释）。
（完整 spec **已迁到规则文件 `workspace/AGENTS.md`**（v0.3，9/19），用户直接改；`src/app.js` 的 `SYSTEM_PROMPT` 只剩应用协议——[动手] 路由、记忆、[记规则]，不是人格）

**鼓励优先**（9/19 夜定）：同学不擅长电脑、容易受挫——AI 要主动鼓励（夸具体，不空洞吹捧）；**帮不了的时候也要鼓励**（先肯定 → 老实说做不到 → 给替代方案），绝不让同学觉得「是我的错」；不说居高临下的话。

**口头禅与底线**（9/19 本人自述）：口头禅「666」「逆天」「不耗！」「hhh」；安慰句式「没事的没事的，加油！你已经很厉害了！真的！我都看到了！」；**底线——不准嘲笑。陪伴就是，接住问题，解决问题**；偶尔轻轻带一句「有空记得来找真的可乐聊天呀」（分身引回真人）。

**干活手人格**（9/19 已注入实测 ✓）：headless 引擎的 persona 在 `~/.dsh/profiles/headless/cordis.patch.yml` → `system-prompt.persona`；汇报从机器人腔（"已完成：1…2…"）变成微信腔（"看完了，就一个文件，特别简单…"）。

**规则文件机制（v0.3 已实现 · 9/19）**：DSH 的 `dsh-agent-instructions` 自动读取 cwd 及祖先目录的 `AGENTS.md` / `CLAUDE.md`（含 `.local` 变体），**实时监听改动**；用户全局位 = `~/.dsh/AGENTS.md`。灯塔直接用它——**一个文件 `workspace/AGENTS.md` 两个消费者，零同步**：
- **聊天侧**：渲染进程每次发送时经 `get-rules` 现读（改完即生效，不用重启），拼进 system prompt 最前（人格 + 同学自己加的规矩）。
- **干活手**：DSH 原生的 cwd 读取，白捡。
- **播种**：首次运行若文件不存在，写入默认人格文本（`main.js` 的 `DEFAULT_RULES`）；**对话现场调教** = chat 模型回 `[记规则] <一行>` → `append-rule` 追加到 `## 后来记下的` → 气泡透明告知「记住了：…」（不弹确认卡：这是 AI 自己的规矩本，不是同学的文件）。
- **验证（9/19 实测）**：① `getRules` 现读 829 字 ✓ ② 问「你是谁」→ 从文件读出的 Cola 分身口吻 ✓ ③ 说「以后回答开头叫我老板」→ 文件追加该行 + 告知气泡 ✓ ④ 下一条回复即叫「老板」（现读生效，无重启）✓ ⑤ 干活手直接 spawn 问「按谁的规矩说话」→ 答「Cola 定在 AGENTS.md 里的规矩」✓

**教学优先**（9/19 定）：同学问「怎么做」= 想学，不是想被代劳——只讲方法和步骤，别抢着做完；拿不准先问一句「帮你弄好，还是你动手我讲方法？」（Cola 本人的教练模式移植）。

**名字 = Cola，头像 = Cola 本人**（9/18 夜定）：AI 是 Cola 的「AI 分身」人格，对同学朋友自带亲切感；头像文件放 `src/assets/avatar.png`（没放时回退字母 C 圆）。被问是不是真人时如实说是 AI。

**表情符号少而精**（9/19 定）：偶尔一个就够，不堆表情。

## Office 工具箱（v0.6 · 9/19）

**验证在先（9/19 实测）**：干活手有 `pwsh` 工具、能跑命令 ✓；它自己也能生成合法 docx（python-docx 产出，我用 Python zipfile 独立验过 OOXML 结构）——**但它靠的是本机装好的 Python 库，同学的电脑上没有 Python**。（⚠️ 更正：我先前说"本机没有 Office"是查错了注册表，实际上 Word/Excel/PowerPoint 16.0 都装着的——现在**真 Office COM 成了我们的验收工具**，比 Python 库严格得多。）"调本机 Office 做事"这条路仍然不作为方案：同学机器上有什么不可知，工具箱必须自带。

**结论：自带工具箱，走 Node**（DSH 本身就跑在 Node 上，打包时必然带运行时；零外部依赖）。

`tools/office.js`（4 个命令）：
| 命令 | 作用 |
|---|---|
| `read <文件>` | docx（段落 + 表格按 ` \| ` 分行）/ xlsx（每表逐行，制表符分隔）/ pptx（每页分段）→ 纯文本 |
| `docx-new <输出> <json>` | 从 JSON 规格生成 Word（title / h1 / h2 / p / bullet / table） |
| `xlsx-new <输出> <json>` | 从 JSON 生成 Excel（多表、表头加粗） |
| `pptx-new <输出> <json>` | 从 JSON 生成 PPT（16:9，标题 + 要点） |

- 依赖：`jszip` / `docx` / `pptxgenjs` / `exceljs`（npmmirror 装，随应用走，同学无感）
- **围栏**：一切路径必须落在 cwd（=工作区）之内，`..`、绝对路径、软链越界一律拒绝（实测三连拒）
- **注入方式**：主进程 `get-engine-hint` 把工具箱绝对路径 + 用法 + 「**用户的电脑上没有 Python，别用 Python/pip**」拼在每次计划/执行任务前面（`src/app.js`）
- **产出物独立验证（9/19）**：python-docx / openpyxl / python-pptx 都能正确读回我们生成的三件套（标题、段落、项目符号、表格、多工作表、幻灯片分页全部对得上）；`docProps/core.xml` 的生产者指纹 = 我们的库（`Un-named`）而非 `python-docx` → 证明干活手确实走的工具箱
- **E2E 实测**：「帮我做个 Word…」→ [动手] → 计划 → 确认卡 → 执行 → 文件落地（内容独立验过）；「读书清单.docx 写了什么」→ **不弹卡直接答**（见下）

### 对抗审查与修复（9/19 · 全新上下文 agent 专挑刺）

带着"想办法搞坏它"的指令跑了一轮独立审查，咬出 5 条，逐条修完并回归：

| # | 问题 | 修法 | 回归证据 |
|---|---|---|---|
| 1 | **围栏可绕**（High）：`safe()` 只对已存在路径做 realpath → 经 junction **往开机启动目录写进了文件** | 改查"最深的已存在祖先"的 realpath（不存在的末段也查父目录） | 复现命令重跑：junction 读/写**双双被拒**，围栏外目录 0 文件 |
| 2 | **只读判定不可靠**（High）：`[只读]` 是模型自评，执行侧无机制约束 | ① 只读分支改传"计划"而非用户祈使句，并标注"这是问题不是命令" ② 计划正文出现写入意图（带否定句剥离）→**强制弹卡**（fail-safe：错判成要写只多一张卡） | 7 组判定用例全对，含审查员那条误标案例 |
| 3 | **空表格行生成 Word 打不开的 docx**（Med-High） | 空行兜成空单元格 | 真 Word COM：修前 FAIL（"用文本恢复转换器打开"）→ 修后 **OPEN ok** |
| 4 | **read 表格串行 + 实体不解码**（Medium） | 切分带边界（`<w:tr` 会连 `<w:trPr` 一起切中）+ XML 实体解码 | `A & B <tag> 100% > 50` 与 2×2 表格读回全部正确 |
| 5 | 输出不校验扩展名，可把 `AGENTS.md` 覆盖成 zip | 写出路径加扩展名白名单 | 拿 `AGENTS.md` 当输出名 → 被拒 |

审查员**没撬开**的（如实记录）：`..`／绝对路径／UNC／8.3 短名／subst 盘／大小写变体全被拦；22 份畸形规格（null、20000 层嵌套、对象当行）无崩溃；坏文件读取全优雅失败。三条真 Office COM 验证（Word/Excel/PowerPoint 打开我们生成的三件套）全 OPEN ok。

**残留风险（留给 v0.7 围栏硬化）**：`[只读]` 分支仍是"提示词约束"而非机制约束——真焊死要给 DSH 配只读 profile / 沙箱（`dsh-permission-presets`、`dsh-fs-sandbox` 都在，v0.7 接）。

## 内嵌 DSH（v0.7 · 9/19）

目标：把干活引擎身上三根外挂管子全拔掉，让它在**没有 Hub、没有系统 Node、没有 `~/.dsh`** 的机器上也能跑。

| 外挂 | 以前 | 现在 |
|---|---|---|
| DSH 包 | 硬编码指向 `Z:/laragon/hub/node_modules/@deepseek-ai/dsh` | 装进应用自己的 `node_modules`（npmmirror，240 包） |
| Node 运行时 | `spawn('node', …)` 用系统 Node | `spawn(process.execPath, …)` + `ELECTRON_RUN_AS_NODE=1`——**用应用自带的 Electron 当 Node**，零额外体积 |
| 配置树 | `~/.dsh/profiles/headless/`（人格 patch 在家目录） | `DSH_HOME` 指向应用自己的 `dsh-home/`（打包态落 userData），首次运行**播种** profile + 人格 patch（跟规则文件一个套路） |
| 工具箱调用 | `node tools/office.js …` | 启动时**生成 `dsh-home/office.cmd` 壳**（路径按本机写死），干活手照抄它——同学机器上没有 `node` 命令 |

**极端环境验收（决定性证据）**：把 `PATH` 剥到只剩 `C:\Windows\system32;C:\Windows`（**没有 node、没有 python**）启动应用 → ① 只读任务答对 ✓ ② 写任务走完确认卡、用壳跑到工具箱、产出落到 `成品/` ✓ ③ **真 Word 打开产出** ✓。Dsh 状态落点也实锤迁移：新会话进 `dsh-home/sessions/`，旧 `~/.dsh` 会话数停在 25 不再增长 ✓。

**踩坑记录（值一条宪法级教训）**：Electron 当 Node 跑的子进程**继承了上游的 `CHROME_CRASHPAD_PIPE_NAME`**，于是它仍去注册 crashpad，被拒后把 `debug.log` 拉在 cwd——**也就是同学的工作区里**。修法：spawn 时把 `CHROME_*` / `ELECTRON_*` 环境变量一律摘掉。⚠️ 顺带钉死一条：**`ELECTRON_RUN_AS_NODE` 模式下 electron.exe 就是纯 Node，不认 Chromium 开关**（我一度加 `--disable-breakpad`，直接 `bad option` 把 spawn 打挂）。

### 工作区体验（9/19 用户提的三件事）

| 想法 | 做法 | 状态 |
|---|---|---|
| 拖文件进对话框 → 自动进工作区 | 拖放事件拦掉默认导航；`webUtils.getPathForFile` 拿真实路径 → 主进程**复制**（不移动）进工作区，同名自动 `(2)`；已在工作区内的、文件夹一律跳过 | ✅ 实测：单文件/重名/工作区内/文件夹四种情况全对；CDP `Input.dispatchDragEvent` 模拟整条链路通过；拖入时聊天区亮 Signal Blue 虚线框 |
| 成品单独放，原文件不动 | 工作区建 `成品/` 子夹；**引擎提示加「成品规矩」**：产出物一律进 `成品/`、原文件只读（除非用户明说"直接改这个文件"）、临时规格文件用完删 | ✅ 实测：让它做 Word → 计划里自己就写"放到成品夹、不动原文件" → 落盘 `成品/读书笔记.docx`，真 Word 打开 ✓，原文件 mtime 未变 ✓ |
| AI 自动整理工作区 | **不做自动**（用户 9/19 拍板方向：自动挪/改同学的文件 = 正是"下次找不到"的病根，且跟确认卡红线打架）。改为：AI 自己不搞乱（临时文件用完删）+ 同学开口"帮我整理"时才整理（走确认卡）+ 成品永远进 `成品/` | 决定记录 |

### 只读取不打扰（补齐 9/18 红线）
计划阶段第一行自报 `[只读]` / `[要写]`：只读 → **跳过确认卡**直接执行（执行提示词同时硬约束"禁止写任何文件"，误判的最坏后果 = 它多问一句，绝不会没确认就改文件）；要写 → 老规矩弹卡。⚠️ 提示词必须写明"判断的是**用户这件事**要不要动文件，跟你现在只做计划无关"——漏了这句，干活手会把"做个 Word"也判成只读（实测踩到并修复）。

## 显示规矩（v0.6 补）

聊天气泡是**轻排版**：`**加粗**` 和 `` `代码` `` 正常显示（8px 小圆角蓝底），其余 Markdown 记号原样露出。渲染全程 textContent 拼接、绝不 innerHTML（模型输出不可信）；`<img onerror=…>` 实测只当文本。两个模型的提示词都要求尽量别写 Markdown（像发微信，要分条就换行写 1. 2. 3.）。

## 干活确认（产品红线 · 9/18 夜定）

AI 动文件前，对话里弹**确认卡**：「我要把 A 改成 B，可以吗？ [可以] [先别]」——说人话、可回退，用户点头才动手。

**范围**（9/18 夜定）：**改、删、新建**要确认；**只读取不打扰**——频率和安全感的最优平衡。

**已实现（2026-09-19 · 两段式）**：① headless 只理方案（prompt 禁写：「先查看、只输出计划」）② 计划进确认卡 `[可以，动手] [先别]` ③ 同意后第二次 spawn 执行。E2E 实测：先别 → 文件原封不动 + 「好，那就先不动」；可以 → 16 秒完成并自复查。计划质量意外地好（DSH 会在计划里主动说「你说声动手我就改」）。

## 路线图（计划单）

| 阶段 | 内容 | 状态 |
|---|---|---|
| v0 | 壳 + 极简聊天 UI + 真流式对话 | ✅ 2026-09-18 夜跑通 |
| v0.1 | 工作区机制：exe 同目录「工作区」文件夹（不可写则回退文档目录），同学只点按钮；**确认卡两段式（已接线 ✅ 9/19）** | 🔨 进行中 |
| v0.2 | 聊天记录持久化 + **上下文压缩**：长对话时 AI 主动告知「我整理一下前面的」，摘要替换旧历史，重要记忆落 `记忆.md` | ✅ 2026-09-19 完成（实测：33k→3k 字符、重启还原、记忆精准提取） |
| v0.3 | 规则文件接入（`workspace/AGENTS.md` 用户写，右上角规则按钮可打开）+ 对话现场调教（AI 把新要求写进规则文件） | ✅ 2026-09-19 完成（五条实测见上；人格已从 app.js 迁出） |
| v0.4 | 「深度思考」开关：默认快模型，同学遇难题可切 `deepseek-v4-pro` | ✅ 2026-09-19 完成（标题栏文字开关，亮=开；localStorage 记住选择，重启还原实测 ✓；开=收 reasoning_content 只显示「深度思考中…」占位、不传 temperature、max_tokens 8192；关=flash。实测：开走 v4-pro（9.9>9.11 答对）、关走 flash（fetch 拦截器实锤）） |
| v0.5 | 外观：自定义壁纸（默认内置深海渐变）+ 玻璃透明度（默认预置 + 同学可自调） | ✅ 2026-09-19 完成（标题栏外观按钮 → 浮层：换背景图 / 恢复默认 + 玻璃透明度滑杆 30–95%，点外面自动收起。壁纸**复制进 `data/wallpaper.*`**（原图之后挪走删掉都不怕），渲染端拿 data URL + 压一层深色保对比度；滑杆驱动 `--surface` / `--surface-user`，气泡与卡片同时变。两项都进 localStorage；实测：应用 ✓ 拖杆即时变 ✓ 重启还原 ✓（正规退出）/ 恢复默认删文件回深海渐变 ✓。⚠️ 原生选图对话框本身没法 CDP 自动化，只走了代码审查；对话框之后的下游全链路已实测） |
| v0.6 | 引擎接入：Office 读写（docx/xlsx/pptx）+ 技术任务 ← 先验证 UI↔DSH 通道（3080 API / headless / 都不行则自写工具环） | ✅ 2026-09-19 完成（工具箱 `tools/office.js` + 提示注入；实测全链路见「Office 工具箱」节） |
| v0.7 | DSH 内嵌（exe 内部启动） | ✅ 2026-09-19 完成（三件套全自带，见「内嵌 DSH」节；极端环境实测通过）· ⚠️ 留给 v1.0：`node_modules` + `tools/` 打包时要解包到 asar 外面（外部进程读不了 asar） |
| v1.0 | 打包 exe + 说明书（**软件内新手引导 + 一张可转发的图文**） | 计划（9/19 定） |
| v2.0 | **Obsidian 接入**：先做**自带 markdown 笔记区**（文件天然兼容 Obsidian）；检测本机已装则打通，未装则 AI 引导安装。⚠️ 打包 Obsidian 安装包随软件分发 = 再分发闭源软件（需先核条款）+ 版本过期 + 挪目录即坏的坑——倾向"引导下载"或"发前远程代装"；v2.0 开工时定 | 远景（9/19 立项） |

## v0 不做（留结构）

- 多会话历史、设置面板、Markdown 渲染、打包分发
- 干活场景（文件操作）——产品红线：动文件前必须人话确认

## 文件结构

```
lighthouse/
├─ main.js        # 主进程：开窗 / key / 窗口控制 / 干活引擎 spawn / 数据落盘
├─ preload.js     # IPC 桥（最小暴露：窗口控制 · 任务 · 聊天存取 · 记忆）
├─ src/
│   ├─ index.html # 结构（CSP 只放行 api.deepseek.com）
│   ├─ style.css  # 深海安静版设计语言
│   └─ app.js     # 单会话聊天 + 流式 + 确认卡 + 压缩 + 规则（现读 / [记规则]）
├─ tools/office.js # 干活手的 Office 工具箱（v0.6，Node 跑，围栏锁工作区）
├─ dsh-home/      # 内嵌 DSH 的配置树（gitignore；打包态落 userData）：profiles/headless + 播种的 office.cmd 壳
├─ node_modules/  # 依赖全自带：electron + dsh（240 包）+ office 四件套
├─ data/          # 运行数据（gitignore）：chat.json · 记忆.md · wallpaper.*（自定义壁纸）
├─ workspace/     # 工作区（围栏，gitignore）：同学的文档 + AGENTS.md（规则文件，v0.3）+ 成品/（产出一律放这）
└─ DESIGN.md
```
