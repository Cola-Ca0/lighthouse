# Lighthouse · 灯塔 — 设计（v0）

> 2026-09-18 立项构思会定稿。目标用户：**水平很低的同学**（微信级——不能碰命令行/配置/英文术语）。

## 一句话

一个说人话的 AI 小助手：桌面窗口，打开即聊，零学习成本。

**目标能力**（2026-09-18 用户定）：聊天问答 + 写东西 + **读写 PPT / Word / Excel** + 技术任务；AI 的活动范围锁在**工作区文件夹**内（围栏红线：只有放进去的文档才能被读改）；DSH 引擎只在 exe 内部启动，不出现网页形态；对话规则（skill）由用户撰写。

## 约束（用户定的红线）

- 内存 ≤ 300MB，CPU/GPU 克制 → 无粒子、无循环动画，动效只做一次性微交互
- 审美不能差 → 深海设计语言（与 Cola Hub / 博客同一宇宙：深底 / 玻璃 / Signal Blue）
- 分发态零配置：key 由站长提供并预置，用户无感知

## 形态

- Electron 无边框窗口（480×760 竖版，自绘标题栏）
- **单会话**：无历史列表、无侧栏、无设置——像微信跟一个人单聊
- 纯手写 HTML/CSS/JS，零框架（与博客/Hub 一致）

## 技术决定（2026-09-18）

| 决定 | 理由 |
|---|---|
| 前端直连 `api.deepseek.com`，主进程不做 API 代理 | 参考 yif2012/deepseek-chat-electron 验证 CORS 开放，实现最简 |
| SSE 解析带缓冲区 | 参考项目用 `split('\n')` 有跨 chunk 丢数据的 bug，我们修正 |
| 不显示 `reasoning_content` | 对低水平用户，一句「深度思考中…」占位比满屏思维链友好（v0.4：开着深度思考时收流但只显示占位，正文一到就替换） |
| key 读取：本地 config.json → Hub settings.json 兜底 | 开发态零配置；分发态预置 config.json |
| 模型：默认 `deepseek-flash` + 「深度思考」开关切 `deepseek-v4-pro`（2026-09-19 修正：`deepseek-chat` 已失效） | 日常快且便宜；同学遇难题自己按开关升级（9/19 定；v0.4 已接线，实测 models 端点只此两个 id） |
| 长对话：超阈值自动压缩，**AI 主动告知**（不悄悄丢历史） | 同学不知道「上下文」为何物，透明化处理 + 重要记忆落文件才安全（9/19 定） |

## 引擎通道验证（2026-09-19 上午实测）

**结论：干活引擎 = DSH headless spawn；聊天 = DeepSeek API 直连 —— 混合架构。**

| 测试 | 结果 | 用时 |
|---|---|---|
| T1 纯聊天 `dsh --profile headless "只回复：收到"` | ✅ stdout 干净输出 | 5s |
| T2 带工具（写文件） | ✅ 文件真创建 + 自读取验证 + 汇报 | 6s |
| T3 跨进程记忆 | ❌ 每次 spawn = 全新会话（源码实锤：每次 `session-<uuid>` 新生成，无 resume 旗标） | — |

- **干活路径**：spawn `dsh --profile headless "<任务>"`（cwd=工作区）；stdout=最终回复、exit code=成功与否、stderr=错误；多轮上下文用**注入式**（历史拼进任务文本——DeepSeek 前缀缓存保证低成本）
- **8 月 MCP 通道的工具挂死问题，headless 通道不存在**（T2 实锤）
- **配置面** = `~/.dsh/profiles/headless/cordis.patch.yml`（现为空 `[]`）——沙箱/权限/人格/技能全在这层挂
- **✅ E2E 干活闭环（同日 10:20 实测）**：灯塔里说「建 demo.txt」→ 路由 `[动手]` → 后台 spawn DSH（cwd=工作区）→ 「好，这就去弄…」→ 15 秒后结果气泡 + 文件真实落地。学生视角 = DSH 完全隐形。

**DSH 现成模块 ↔ 产品红线对照**（180+ 包冰山）：

| 红线 | DSH 模块 |
|---|---|
| 工作区围栏 | `dsh-sandbox` / `dsh-fs-sandbox` / `dsh-sandbox-windows-acl` |
| 确认卡 | `dsh-user-approval` / `dsh-tool-ask-user` / `dsh-permission-presets` |
| 上下文压缩 | `dsh-compaction`（UI 层负责"主动告知"） |
| 规则.md | `dsh-skill-filesystem` / `dsh-agent-instructions` / `dsh-persona` |
| 计划→确认→执行 | `dsh-plan-mode` |

⚠️ `HUB_CAPABILITY` env 在 DSH 侧无实现（Hub 留的坑位）——权限不能靠它，要靠 cordis patch 或 UI 层两段式。

## AI 人格（Cola 本人口吻）

**说话风格 ≈ Cola 本人**（9/19 夜定）：短句口语直接、不客套 / 先结论后展开 / 轻度吐槽幽默（「这波可惜」「先别半场开香槟」味，偶尔一句别堆）/ 讲技术打比方说人话 / 不会就说不会；对不懂电脑的同学保留耐心版（操作一步步、术语顺带解释）。
（完整 spec **已迁到规则文件 `workspace/AGENTS.md`**（v0.3，9/19），用户直接改；`src/app.js` 的 `SYSTEM_PROMPT` 只剩应用协议——[动手] 路由、记忆、[记规则]，不是人格）

**鼓励优先**（9/19 夜定）：同学不擅长电脑、容易受挫——AI 要主动鼓励（夸具体，不空洞吹捧）；**帮不了的时候也要鼓励**（先肯定 → 老实说做不到 → 给替代方案），绝不让同学觉得「是我的错」；不说居高临下的话。

**口头禅与底线**（9/19 本人自述）：口头禅「666」「逆天」「不耗！」「hhh」；安慰句式「没事的没事的，加油！你已经很厉害了！真的！我都看到了！」；**底线——不准嘲笑。陪伴就是，接住问题，解决问题**；偶尔轻轻带一句「有空记得来找真的可乐聊天呀」（分身引回真人）。

**干活手人格**（9/19 已注入实测 ✓）：headless 引擎的 persona 在 `~/.dsh/profiles/headless/cordis.patch.yml` → `system-prompt.persona`；汇报从机器人腔（"已完成：1…2…"）变成微信腔（"看完了，就一个文件，特别简单…"）。

**规则文件机制（v0.3 已实现 · 9/19）**：DSH 的 `dsh-agent-instructions` 自动读取 cwd 及祖先目录的 `AGENTS.md` / `CLAUDE.md`（含 `.local` 变体），**实时监听改动**；用户全局位 = `~/.dsh/AGENTS.md`。灯塔直接用它——**一个文件 `workspace/AGENTS.md` 两个消费者，零同步**：
- **聊天侧**：渲染进程每次发送时经 `get-rules` 现读（改完即生效，不用重启），拼进 system prompt 最前（人格 + 同学自己加的规矩）。
- **干活手**：DSH 原生的 cwd 读取，白捡。
- **播种**：首次运行若文件不存在，写入默认人格文本（`main.js` 的 `DEFAULT_RULES`）；**对话现场调教** = chat 模型回 `[记规则] <一行>` → `append-rule` 追加到 `## 后来记下的` → 气泡透明告知「记住了：…」（不弹确认卡：这是 AI 自己的规矩本，不是同学的文件）。
- **验证（9/19 实测）**：① `getRules` 现读 829 字 ✓ ② 问「你是谁」→ 从文件读出的 Cola 分身口吻 ✓ ③ 说「以后回答开头叫我老板」→ 文件追加该行 + 告知气泡 ✓ ④ 下一条回复即叫「老板」（现读生效，无重启）✓ ⑤ 干活手直接 spawn 问「按谁的规矩说话」→ 答「Cola 定在 AGENTS.md 里的规矩」✓

**教学优先**（9/19 定）：同学问「怎么做」= 想学，不是想被代劳——只讲方法和步骤，别抢着做完；拿不准先问一句「帮你弄好，还是你动手我讲方法？」（Cola 本人的教练模式移植）。

**名字 = Cola，头像 = Cola 本人**（9/18 夜定）：AI 是 Cola 的「AI 分身」人格，对同学朋友自带亲切感；头像文件放 `src/assets/avatar.png`（没放时回退字母 C 圆）。被问是不是真人时如实说是 AI。

**表情符号少而精**（9/19 定）：偶尔一个就够，不堆表情。

## 干活确认（产品红线 · 9/18 夜定）

AI 动文件前，对话里弹**确认卡**：「我要把 A 改成 B，可以吗？ [可以] [先别]」——说人话、可回退，用户点头才动手。

**范围**（9/18 夜定）：**改、删、新建**要确认；**只读取不打扰**——频率和安全感的最优平衡。

**已实现（2026-09-19 · 两段式）**：① headless 只理方案（prompt 禁写：「先查看、只输出计划」）② 计划进确认卡 `[可以，动手] [先别]` ③ 同意后第二次 spawn 执行。E2E 实测：先别 → 文件原封不动 + 「好，那就先不动」；可以 → 16 秒完成并自复查。计划质量意外地好（DSH 会在计划里主动说「你说声动手我就改」）。

## 路线图（计划单）

| 阶段 | 内容 | 状态 |
|---|---|---|
| v0 | 壳 + 极简聊天 UI + 真流式对话 | ✅ 2026-09-18 夜跑通 |
| v0.1 | 工作区机制：exe 同目录「工作区」文件夹（不可写则回退文档目录），同学只点按钮；**确认卡两段式（已接线 ✅ 9/19）** | 🔨 进行中 |
| v0.2 | 聊天记录持久化 + **上下文压缩**：长对话时 AI 主动告知「我整理一下前面的」，摘要替换旧历史，重要记忆落 `记忆.md` | ✅ 2026-09-19 完成（实测：33k→3k 字符、重启还原、记忆精准提取） |
| v0.3 | 规则文件接入（`workspace/AGENTS.md` 用户写，右上角规则按钮可打开）+ 对话现场调教（AI 把新要求写进规则文件） | ✅ 2026-09-19 完成（五条实测见上；人格已从 app.js 迁出） |
| v0.4 | 「深度思考」开关：默认快模型，同学遇难题可切 `deepseek-v4-pro` | ✅ 2026-09-19 完成（标题栏文字开关，亮=开；localStorage 记住选择，重启还原实测 ✓；开=收 reasoning_content 只显示「深度思考中…」占位、不传 temperature、max_tokens 8192；关=flash。实测：开走 v4-pro（9.9>9.11 答对）、关走 flash（fetch 拦截器实锤）） |
| v0.5 | 外观：自定义壁纸（默认内置深海渐变）+ 玻璃透明度（默认预置 + 同学可自调） | 计划（9/19 定） |
| v0.6 | 引擎接入：Office 读写（docx/xlsx/pptx）+ 技术任务 ← 先验证 UI↔DSH 通道（3080 API / headless / 都不行则自写工具环） | 待验证 |
| v0.7 | DSH 内嵌（exe 内部启动） | 待定 |
| v1.0 | 打包 exe + 说明书（**软件内新手引导 + 一张可转发的图文**） | 计划（9/19 定） |
| v2.0 | **Obsidian 接入**：先做**自带 markdown 笔记区**（文件天然兼容 Obsidian）；检测本机已装则打通，未装则 AI 引导安装。⚠️ 打包 Obsidian 安装包随软件分发 = 再分发闭源软件（需先核条款）+ 版本过期 + 挪目录即坏的坑——倾向"引导下载"或"发前远程代装"；v2.0 开工时定 | 远景（9/19 立项） |

## v0 不做（留结构）

- 多会话历史、设置面板、Markdown 渲染、打包分发
- 干活场景（文件操作）——产品红线：动文件前必须人话确认

## 文件结构

```
lighthouse/
├─ main.js        # 主进程：开窗 / key / 窗口控制 / 干活引擎 spawn / 数据落盘
├─ preload.js     # IPC 桥（最小暴露：窗口控制 · 任务 · 聊天存取 · 记忆）
├─ src/
│   ├─ index.html # 结构（CSP 只放行 api.deepseek.com）
│   ├─ style.css  # 深海安静版设计语言
│   └─ app.js     # 单会话聊天 + 流式 + 确认卡 + 压缩 + 规则（现读 / [记规则]）
├─ data/          # 运行数据（gitignore）：chat.json · 记忆.md
├─ workspace/     # 工作区（围栏，gitignore）：同学的文档 + AGENTS.md（规则文件，v0.3）
└─ DESIGN.md
```
